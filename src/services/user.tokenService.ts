import { Injectable } from '@nestjs/common';

@Injectable()
export class TokenService {
  constructor() {}

  /*
   *assign token service and also a service to read and check if a token is expired and a service to validate tokens
   * */
}
